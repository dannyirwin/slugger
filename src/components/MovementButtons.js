import { useRef } from "react";

export default function MovementButtons({ setMovementButtons }) {
  return (
    <div>
      <button>Up</button>
      <button>Down</button>
      <button>Left</button>
      <button>Right</button>
    </div>
  );
}
